package com.everybuddy.domain.media.entity;

public enum MediaType {
    IMAGE,      // jpg, png, gif, webp, heic 등
    VIDEO,      // mp4, mov, avi, webm 등
    AUDIO,      // mp3, wav, m4a, aac 등 (번역 대상)
    DOCUMENT    // pdf, txt, doc, docx, xls, xlsx, ppt, pptx, zip, rar 등
}
